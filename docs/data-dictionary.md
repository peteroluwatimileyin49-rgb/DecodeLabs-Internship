# Data Dictionary

This document records the fields used in the e-commerce sales dataset. Update the table with the exact column names and definitions from the final workbook before submitting the project.

| Column | Description | Data type | Validation or notes |
|---|---|---|---|
| `OrderID` | Unique identifier for an order. | Text or integer | Should contain no duplicates. |
| `OrderDate` | Date on which the order was placed. | Date | Standardized to `YYYY-MM-DD`. |
| `CouponCode` | Coupon or promotion code associated with the order. | Text | Missing values were handled during cleaning. |
| *Add remaining columns* | Add the exact meaning of each remaining workbook column. | *Add type* | Document allowed values and validation rules. |

## How to complete this document

1. Open the final cleaned workbook.
2. Copy each exact column name into the table.
3. Record the business meaning and data type.
4. Document any calculated fields, allowed values, or special handling.
5. Keep this document synchronized with the final workbook.
